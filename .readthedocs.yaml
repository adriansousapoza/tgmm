version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.11"  # Use a more stable Python version for docs

mkdocs:
  configuration: mkdocs.yml

python:
  install:
    - requirements: docs/requirements.txt
    - method: pip
      path: .
